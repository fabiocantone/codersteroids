# Open Questions

## Context7 Runtime Integration

Should this plugin ship an MCP configuration for Context7, or only instruct agents to use an existing Context7 MCP/CLI setup?

Current MVP decision: do not require Context7 installation. Prefer Context7 when available and require official-doc fallback otherwise.

## Live Benchmark Results

The plugin has not yet been scored against Superpowers with the benchmark prompts. Do not claim superiority until `benchmarks/results/` contains completed runs.
